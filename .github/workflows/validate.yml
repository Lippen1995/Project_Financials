name: Validate

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      AUTH_SECRET: ci-only-auth-secret-that-is-not-used-outside-ci
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/projectx?schema=public
      NEXTAUTH_URL: http://localhost:3000

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: projectx
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d projectx"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check tracked environment and secret files
        run: npm run security:check-env-files

      - name: Check API input validation inventory
        run: npm run security:check-api-inputs

      - name: Protect financial source-access baseline
        run: npm run financials:check-source-access

      - name: Audit production and development dependencies
        run: npm run security:audit

      - name: Apply database migrations
        run: npm run db:migrate:deploy

      # Catches migration-checksum drift and raw-SQL indexes that
      # `prisma migrate diff` would propose dropping. Runs after deploy so it
      # checks the state migrations actually produce.
      - name: Check migration integrity
        run: npm run db:check-migrations

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
